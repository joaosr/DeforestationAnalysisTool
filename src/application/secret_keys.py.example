# CSRF- and Session keys.

CSRF_SECRET_KEY = ''
SESSION_KEY = ''


# Fusion Tables API keys.

FT_CONSUMER_KEY = ''
FT_CONSUMER_SECRET = ''
FT_TOKEN = '' 
FT_SECRET = ''


# Earth Engine API keys.

# An email, typically a service account email (not ID).
EE_ACCOUNT = ''
# A PEM secret key file. This can be generated by download a P12 key from
# the API console and then running:
# openssl pkcs12 -in xxxxxxxxxxx.p12 -nodes -nocerts > privatekey.pem
# Not that due to a bug in oauth2client, the key needs to be edited to
# remove the first 4 lines of the resulting PEM (everything before -----BEGIN).
# https://code.google.com/p/google-api-python-client/issues/detail?id=249
EE_PRIVATE_KEY_FILE = ''
